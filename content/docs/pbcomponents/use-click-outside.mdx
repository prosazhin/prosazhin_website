---
title: useClickOutside
description: 'Вызывает callback при клике или касании вне переданных элементов.'
---

import ComponentAPI from '@/components/docs/ComponentAPI';

Вызывает callback при клике или касании вне переданных элементов.

## Использование

```tsx
'use client';

import { useRef, useState } from 'react';
import { useClickOutside } from '@prosazhin/pbcomponents';

function Example() {
  const [open, setOpen] = useState(true);
  const ref = useRef<HTMLDivElement>(null);
  useClickOutside({ refs: [ref], callback: () => setOpen(false) });
  return open ? <div ref={ref}>Menu</div> : null;
}
```

## API хука

<ComponentAPI
  name='useClickOutside'
  locale='ru'
/>
