---
title: Button
description: 'An action or link with size, color, theme and loading state.'
---

import ComponentShowcase from '@/components/docs/ComponentShowcase';
import ComponentAPI from '@/components/docs/ComponentAPI';

Use `onClick` for an action and `href` for navigation. `loading` shows progress; `disabled` blocks interaction.

## Live example

<ComponentShowcase
  name='Button'
  locale='en'
/>

## Usage

```tsx
import { Button } from '@prosazhin/pbcomponents';

<Button
  size='m'
  theme='filled'
  color='primary'
>
  Button
</Button>;
```

## Component API

<ComponentAPI
  name='Button'
  locale='en'
/>
